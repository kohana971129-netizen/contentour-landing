-- ============================================================================
-- 48_통역사지원서 realtime 활성화 (2026-05-29)
-- ----------------------------------------------------------------------------
-- 목적: 관리자 대시보드 알림 센터(벨)가 통역사 지원서 INSERT/UPDATE를 실시간
--       반영하도록 supabase_realtime 발행(publication)에 테이블 추가.
--       - 신규 지원서 → 즉시 알림 표시
--       - 지원서 처리(status != 'pending') → 즉시 알림 제거
-- 적용: 2026-05-29 Dashboard SQL Editor에서 실행 완료 (Success. No rows returned).
-- 주의: 이미 발행에 포함돼 있으면 "already member of publication" 에러 → 무시 가능.
-- ============================================================================

ALTER PUBLICATION supabase_realtime ADD TABLE "48_통역사지원서";
